

const getData = async function () {
   try {
        const response = await app.get('/')
   } catch (error) {
    
   }
};

const cmsInforServices = {
    getData
};

export default cmsInforServices;